# Types

TypeScript type definitions specific to the API layer. Shared types that are used
across multiple apps belong in `packages/shared/src/types/` instead.

- `express.d.ts` - Express request/response type augmentations (e.g., req.user)
