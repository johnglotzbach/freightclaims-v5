# Validators

Zod schemas for request validation. Each file exports schemas for a specific domain.
These are used with the `validate()` middleware in route definitions.

Naming: `{domain}.validators.ts`
